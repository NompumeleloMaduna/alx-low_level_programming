#include "main.h"

/**
*postitive_or_negative() - prints postive and negative integers
*
* Description - tests function that prints if integer is positive or negative
* Return: 0
*/

void postitive_or_negative()
{
        int i;

        i = 98;
        positive_or_negative(i);

        return (0);
}
